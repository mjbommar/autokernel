"""`autokernel world` — source-rebuilt Debian, Gentoo-style.

See docs/WORLD.md for the design and milestone plan. W1 ships the
manifest (what to rebuild, with which flags) and the planner (source
closure → build waves → cost estimate). The builder, LLM agents, and
watcher land in later milestones.
"""
